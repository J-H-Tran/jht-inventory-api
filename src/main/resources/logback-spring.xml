<!-- src/main/resources/logback-spring.xml -->
<!--
  Uses %X{traceId} to pull the value from MDC into every log line.
  logback-spring.xml (not logback.xml) lets Spring inject environment
  properties like spring.application.name via <springProperty>.
-->
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="inventory-api"/>
    <property name="LOG_PATH" value="logs"/>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} traceId=%X{traceId} method=%X{httpMethod} path=%X{path} status=%X{status} - %msg%n
            </pattern>
        </encoder>
    </appender>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--
              Pattern breakdown:
                %d{...}        — timestamp
                [%thread]      — thread name (useful once async handlers are added in Week 4)
                %-5level       — log level, left-padded to 5 chars
                %logger{36}    — logger name, truncated to 36 chars
                [%X{traceId}]  — MDC value; prints empty brackets if not set (signals a filter bug)
                %msg%n         — the actual log message
            -->
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} traceId=%X{traceId} method=%X{httpMethod} path=%X{path} status=%X{status} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Quieten Hibernate SQL noise; flip to DEBUG locally when needed -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.flywaydb" level="INFO"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver" level="ERROR"/>

    <springProfile name="dev">
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>
        <logger name="com.example.inventory" level="DEBUG"/>
    </springProfile>

<!--    <springProfile name="prod">-->
<!--        <logger name="com.example.inventory" level="INFO"/>-->
<!--    </springProfile>-->

</configuration>