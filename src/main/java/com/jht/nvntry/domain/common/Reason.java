package com.jht.nvntry.domain.common;

public enum Reason {
    RECEIPT,
    RESERVATION_CREATED,
    RESERVATION_FULFILLED,
    RESERVATION_CANCELLED,
    RESERVATION_EXPIRED,
    MANUAL_ADJUSTMENT,
    RETURN
}